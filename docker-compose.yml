version: '3.8'
services:
  ob12api:
    build: .
    ports:
      - "${OB12API_PORT:-8081}:${OB12API_PORT:-8081}"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
